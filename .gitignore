node_modules/

# Personal — points at this machine's own package repos. Ship
# polyrepo.config.example.json instead; each user creates their own via
# `polyrepo setup` (or by copying the example).
polyrepo.config.json
